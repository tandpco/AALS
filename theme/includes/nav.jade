// Navbar
nav.navbar.navbar-transparent(role="navigation")
  .container
    // Brand and toggle get grouped for better mobile display
    .navbar-header
      button.navbar-toggle.collapsed(type="button", data-toggle="collapse", data-target="#bs-example-navbar-collapse-1")
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href="/")
        img(src="assets://images/logo.png", alt="Adopt a Love Story")
    // Collect the nav links, forms, and other content for toggling
    #bs-example-navbar-collapse-1.collapse.navbar-collapse
      ul.nav.navbar-nav.navbar-right
        li(class="{:url.1 == 'about' ? 'active'}")
          a(href="about") About
        li(class="{:url.1 == 'family' ? 'active'}")
          a(href="family") Our Families
        li(class="{:url.1 == 'engage' ? 'active'}")
          a(href="engage") Engage
        li(class="{:url.1 == 'contact' ? 'active'}")
          a(href="contact") Contact
        li
          //@IF :member.id
          a.login.rain(href="#/my/portal" data-action="portal") Dashboard
          //@ELSE
          a.login.rain(href="#/my/login" data-action="login") Login
          //@ENDIF
    // /.navbar-collapse
  // /.container-fixed
